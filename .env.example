# Database Configuration
# For development (SQLite)
DATABASE_URL="file:./dev.db"
# For production (PostgreSQL) - Replace with your actual database URL
# DATABASE_URL="postgresql://username:password@hostname:port/database?schema=public"
# Shadow database for Prisma migrations (PostgreSQL only)
# SHADOW_DATABASE_URL="postgresql://username:password@hostname:port/shadow_database?schema=public"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production-min-32-chars"
JWT_EXPIRES_IN="7d"
REFRESH_TOKEN_EXPIRES_IN="30d"

# Application Configuration
VITE_APP_NAME="CounselorTempo"
VITE_APP_VERSION="1.0.0"
VITE_TEMPO=false
FRONTEND_URL="http://localhost:5173"

# API Configuration
VITE_API_URL="http://localhost:3001"
API_PORT=3001

# Development Configuration
NODE_ENV=development
VITE_BASE_PATH=/

# Security Configuration
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (for future implementation)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# FROM_EMAIL="noreply@counselortempo.com"

# File Upload Configuration (for future implementation)
# MAX_FILE_SIZE=5242880
# ALLOWED_FILE_TYPES="image/jpeg,image/png,image/gif,application/pdf"
# UPLOAD_PATH="./uploads"

# Monitoring and Logging (for future implementation)
# SENTRY_DSN="your-sentry-dsn"
# LOG_LEVEL="info"

# External API Keys (for future integrations)
# OPENAI_API_KEY="your-openai-api-key"
# TWILIO_ACCOUNT_SID="your-twilio-account-sid"
# TWILIO_AUTH_TOKEN="your-twilio-auth-token"
